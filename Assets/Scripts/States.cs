using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ZombieStates {
    Idle = 0,
    Patrol = 1,
    Search = 2,
    Chase = 3,
    Attack = 4,
    Stunned = 5,
    Dead = 6
}